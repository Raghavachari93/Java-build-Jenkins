# Java Build Jenkins Demo

Simple Maven Java app with unit tests to demonstrate a Jenkins Freestyle job building and testing a Java project.

- Build: `mvn -B clean package`
- Tests: JUnit 5 (maven-surefire-plugin)

